import { DisplayCurrency } from './display-currency';

describe('DisplayCurrency', () => {
  it('should create an instance', () => {
    const directive = new DisplayCurrency();
    expect(directive).toBeTruthy();
  });
});
